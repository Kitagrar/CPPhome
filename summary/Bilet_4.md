# Сдача плюсов  

## Структуры  

1)Данные-члены  

+ Открытые члены (или "public-члены") — это члены структуры или класса, к которым можно получить доступ извне этой же структуры или класса  

+ Закрытые члены (или "private-члены") — это члены класса, доступ к которым имеют только другие члены этого же класса.  

2)Агрегатная инициализация  

Она выполняется, когда массив инициализируется рядом значений в фигурных скобках:  

    int i[4] = {0, 1, 2, 3};  

Агрегатная инициализация на самом деле использует копирующую инициализацию для каждого элемента. Поэтому, если попытаться использовать агрегатную инициализацию (как со знаком равенства, так и без него) для нескольких объектов с explicit конструкторами, то для каждого объекта выполняется копирующая инициализация и происходит ошибка компиляции  

+ Лаконично и легко читается  
+ Эффективность  
+ Гибкий  
+ Поддерживает списки инициализаторов  
+ Переносимый  

3)Конструкторы и деструкторы и их этапы работы  

**Конструктор** — это особый тип метода класса, который автоматически вызывается при создании объекта этого же класса. Конструкторы обычно используются для инициализации переменных-членов класса значениями, которые предоставлены по умолчанию/пользователем, или для выполнения любых шагов настройки, необходимых для используемого класса  

+ конструкторы всегда должны иметь то же имя, что и класс (учитываются верхний и нижний регистры)  
+ конструкторы не имеют типа возврата (даже void).  

### Этапы работы конструктора

+ Создается (выделяется память) и иницилизируются данные-члены (через список инициализации)  
+ Выполняется тело констроутора (поля тут не могут быть инициализированы, значение может быть только присвоено)  

**Деструктор** — это специальный тип метода класса, который выполняется при удалении объекта класса.  

+ деструктор должен иметь то же имя, что и класс, со знаком тильда (~) в самом начале  
+ деструктор не может принимать аргументы;  
+ деструктор не имеет типа возврата  

### Этапы работы деструктора

+ Выполняется тело  
+ Уничтожается (через деструкторы, если они есть) данные члены  

4)Функции-члены  

Обычные функции-члены можно использовать в структурах:  

    struct Person
    {
        std::string name;
        void f()
        {
            std::cout << name;
        }
    };
    i
    int main()
    {
        Person p;
        p.f();
    }  

💡 Функция-член может видеть данные своего экземпляра  
⚠️ Функция-член вызывается относительно своего экземпляра  

6)Битовые поля. Объединения  

--Битовые поля: используются при экономии памяти и работе с протоколами и битовыми структурами, и для улучшения читабельности кода  

### Свойства битовых полей

+ Размер битового поля всегда кратен байту. В неиспользуемых байтах находится мусор  
+ Компилятором может быть произведено выравнивание до четного кол-ва байт  
+ Члены битового поля можно использовать также (почти) как и члены обычной структуры  
+ Члены битового поля не адрессуются  
+ Максимальный размер выделяемый под поле не должен превышать размер типа этого поля  

--Объединения: используются при экономии памяти и преобразовании типов  

Объединение способно хранить в пределах одной зарезервированной области памяти различные типы данных. Но в каждый определенный момент времени в объединении хранится только один из этих типов данных и возможно использовать лишь значение этого элемента  

### Свойства объединения

+ Доступ к элементам объединения осуществляется так же как и к элементам обычной структуры  
+ Размер объединения равен размеру максимального элемента  

7)Перечисления с и без области видимости  

Перечисление (или "перечисляемый тип") — это тип данных, где любое значение (или "перечислитель") определяется как символьная константа. Объявить перечисление можно с помощью ключевого слова enum.  

+ `scoped enum` — не зависят от области видимости, можно использовать `static_cast` для приведения к числам  
+ `unscoped enum` — видны извне, можно использовать числа  

### Свойства перечислений (`unscoped`)

+ Общее пространство имен  
+ Каждому перечислителю по умолчанию присваивается целочисленное значения, начиная с нуля  
+ Можно присваивать произвольные целочисленные значения перечислителям. Неопределенные перечислители будут иметь значения на единицу больше, чем предыдущие  
+ Возможна ситуация в которой у разных перечислителей одинаковые значения в пределах одного перичислителя (Опасно)  
+ Целочисленным переменным можно присваивать значения перечислителей (будет произведена неявная конвертация)  
+ При выводе перечислителей они неявно конвертируются в свое целочисленное значение  
+ Компилятор **не будет** неявно конвертировать целочисленные значения в значение перечислителя (Нужно использовать `static_cast`)  
+ Перечисления нельзя вводить через `std::cin`  
+ Можно использовать `static_cast` для конвертирования целочисленного значения в перечислитель  
+ При сравнеии перечислителей оба неявно конвертируются в целочисленные значения (Может вызвать проблемы. Для решения используется `enum class`)  

### Свойства `enum class`

+ Никогда не производится неявное преобразование к целочисленному значению и обратно (Для преобразования используется `static_cast`)
+ Видны только в своем пространстве имен  

8)Глобальная область видимости  

Отсутвие ассоциативности -> `::` -> глобальная область видимости -> `::name`

9)Пространства имен  

Пространство имен определяет область кода, в которой гарантируется уникальность всех идентификаторов. По умолчанию, глобальные переменные и обычные функции определены в глобальном пространстве имен.  

+ namespace myns {…}  
+ Свойство аддитивности  
+ Свойство вложенности  
+ Псевдонимы названий (`namespace Foo = Boo:Doo`)  
+ Анонимные пространства имен  
